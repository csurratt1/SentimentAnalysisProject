# ── Dependencies (Maven manages these now) ──
lib/

# ── Build output ──
target/
CoreNLP/out/
*.class

# ── IDE / editor ──
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
*.swp
*~

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini

# ── Maven ──
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# ── Database credentials (never commit) ──
db.properties

# ── Temp files from .docx extraction ──
corenlp_docx_*/
corenlp_input_*

# ── Scoring output (timestamped runs, regeneratable) ──
output/

# ── Old scattered output files ──
CoreNLP/*_output.txt
CoreNLP/*_stderr.txt
